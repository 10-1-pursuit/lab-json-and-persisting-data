const {index, show} = require("./controllers")
const data = require("./data/products.json")


describe("the Show function", () =>
    test("given the correct array i work", () => {
        expect(show([])).toStrictEqual("there is an error with your array")
}))


